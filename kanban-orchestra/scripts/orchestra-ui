#!/usr/bin/env bash
# Thin wrapper — real implementation lives at $ORCHESTRA_DIR/orchestra-ui/orchestra-ui.py
if [ -z "$ORCHESTRA_DIR" ]; then
    echo "Error: ORCHESTRA_DIR environment variable is not set." >&2
    exit 1
fi
exec python3 "$ORCHESTRA_DIR/orchestra-ui/orchestra-ui.py" "$@"
